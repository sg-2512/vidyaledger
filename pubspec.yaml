name: vidyaledger
description: Smart school fee, concession, reconciliation, and accounting platform for Indian schools.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  supabase_flutter: ^2.15.4
  fl_chart: ^1.2.0
  pdf: ^3.13.0
  printing: ^5.15.0
  intl: ^0.20.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
